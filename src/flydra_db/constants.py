
# FlydraDB root group in HDF5 files.
FLYDRA_ROOT = 'flydra' # Don't change it just yet, it's hardcoded in some places.

# Index created in the DB
INDEX_FILENAME = 'index.h5'

# Where to store temporary indices 
TMP_INDICES_DIR = 'temporary-indices'
